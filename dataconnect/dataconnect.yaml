specVersion: "v1beta"
serviceId: "tagonline"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tagonline-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
